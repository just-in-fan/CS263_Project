Currently, this quicksort implementation fails my correctness test harness with a RecursionError on sorted 
and reverse-sorted inputs (large sizes up to ~5000). I want you to fix this robustness issue. You must still 
follow the constraints in the original instructions.